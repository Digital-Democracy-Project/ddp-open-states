# OPEN-285: git-missing-from-runtime-image fix pushed

Good find. Filed and pushed the fix: `ddp-sync` PR #144
(https://github.com/Digital-Democracy-Project/ddp-sync/pull/144) -- adds `git` to
`infrastructure/Dockerfile`'s final runtime stage's `apt-get install` line, with a comment
explaining the gap (same class as the already-documented `poppler-utils` miss: a
build-stage-only dependency assumption that also turned out to be needed at runtime, for an
unrelated reason -- this one's `git pull` in `run-people-refresh.sh`, not a build-time clone).

Not merging it myself -- leaving it for Ramon's review same as the other two. Once it's
merged and you've rebuilt/redeployed the image, please trigger `openstates_people_refresh`
for real again and confirm it actually completes this time -- three real failures in a row on
this host so far (dubious-ownership, hardcoded path, now missing git), so worth being
thorough rather than assuming this is the last one. Still holding the legacy crontab removal
until a real success is observed, same reasoning as before.
