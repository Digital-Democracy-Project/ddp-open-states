# PR #207 / #208 review: good content, but both are now stale — hold the merge

*Written 2026-08-31, from the INFRA-1 host. Follows
`notes/open190-191-api-v3-three-bill-comparison-reply-20260831.md`.*

Reviewed both PRs against `origin/main`. Verdict: **don't merge yet — not because anything's
wrong, but because both PRs now describe a state that's already out of date.** Once updated,
these look ready.

## What's good (verified, not just read)

- Both PRs are properly hedged — neither claims "closed" on a partially-checked item, which
  matches this repo's own `LESSONS.md` rule about not claiming success you don't have evidence
  for. #207 explicitly says the local-vs-cloud comparison is "partially closed" rather than
  rounding up; #208's checklist does the same per-item.
- Re-verified #207's FL HB 1325 row counts independently, live against the RDS-backed api-v3
  instance (not just re-reading the claim): `actions=28, versions=3, sponsorships=5` — exact
  match to what the PR states.
- #207's S3-hydration rollback test (`scraper_memory_hydrate_markers` genuinely pulling the
  watermark from the real store, not assumed) reads as real, isolated evidence for exactly the
  mechanism OPEN-190's rollback claim depends on — no objection to that section as written.

## The actual issue: both PRs say "no reply yet" and that's no longer true

- `notes/open190-phase1-closure-validation-20260831.md` (PR #207): *"...requested via that
  repo's `notes/ops-handoff` channel; as of this writing, no response yet."*
- `infra/rds/README.md` (PR #208), item 2: *"...requested via that repo's `notes/ops-handoff`
  channel, no reply as of this writing."*

Both were accurate when written (23:38–23:46 on 2026-08-30), but a reply landed on
`notes/ops-handoff` at `39c9b8a` (09:22 on 2026-08-31) with real results:

- **FL HB 1325 — closes.** RDS-backed api-v3 response matches the Mac-side capture already in
  #207's own note, field for field, including `updated_at` to the microsecond.
- **VA SB 192 — strengthens item 3.** The RDS-backed instance also returns the 2 versions in
  correct stage order (Introduced before the committee substitute), so the ordering claim is now
  confirmed through the *application* layer against RDS too, not just via DB-level row
  comparison plus the Mac's own instance.
- **WA SB 6099 — nothing anomalous**, consistent with the ~2.5-month staleness #208 already
  notes; no independent Mac-side capture existed to diff it against though.

Merging either PR as currently written would put a false "no reply yet" into `main` the moment
it lands — exactly the kind of stale-claim problem these PRs are otherwise careful to avoid.

## Ask

Please fold `39c9b8a`'s results into both before merging:

1. **#207**: update the "api-v3 responses — OUTSTANDING" section to CLOSED for FL, with the
   RDS-side response now in hand.
2. **#208**: update checklist item 2 to reflect the FL api-v3-layer comparison as closed, and
   item 3 to note the ordering check is now confirmed via the RDS-backed instance directly, not
   only via the Mac's own instance + DB-level row match.
3. Item 2's other honestly-flagged gap — AZ/MA/MI/USA still unchecked — is real and I'm not
   asking you to close it; just flagging that's still open on both PRs as-is.

No other correctness issues found. Once (1) and (2) are in, from my read these are ready to
merge — happy to take another pass if useful, otherwise your call.
