# OPEN-285/286 progress: real fixes for both, one real blocker on the credential itself

With Ramon's explicit go-ahead on both.

## OPEN-285: run-people-refresh.sh's hardcoded Mac path -- fixed

Real root cause, not the same one as the legacy cron's: `SCRIPT_DIR` was hardcoded to
`/Users/agentsmith/Developer/repos/ddp-open-states`, so `openstates_people_refresh` has
**never actually succeeded on this EC2 host** -- its one recorded run (2026-09-06) failed
instantly (`0.0s`, `exit_code_1`, `source: No such file or directory`). Fixed to match
`run-scrape.sh`'s own self-locating pattern (`$(dirname "${BASH_SOURCE[0]}")`) rather than a
hardcoded path or another `OPENSTATES_ROOT` env read. Pushed as
`fix/open285-run-people-refresh-hardcoded-mac-path` (`39dc858`) on `ddp-open-states` -- not
merged myself (outside my scoped merge authority), please review/merge alongside your own
PR #375.

**Please hold off on merging/deploying #375 (removing the legacy cron) until this lands and a
real successful run is observed** -- confirmed both mechanisms were broken here, so removing
the cron first would leave zero working people-refresh path on this host in the gap.

Side note, unrelated to what was asked: `render-env.sh` and two other files under
`/opt/ddp-sync/infrastructure/` were never actually committed to the `ddp-sync` repo -- exist
only as local files on this host. Real config-drift gap (OPEN-247's class), not touched, just
flagging.

## OPEN-286: Slack/CAMS alerting -- mechanism fixed and verified, wrong credential used

Real, durable fix landed: `slack_bot_token` added to the `ddp-sync/credentials` Secrets
Manager secret (Ramon did this himself, directly, not via a grant to this host's role --
he didn't want the EC2 instance role able to write that shared multi-key secret, reasonable
call), `render-env.sh`'s `KEY_MAP` updated to map it to `SLACK_BOT_TOKEN`, `ddp-sync` restarted.
Confirmed the whole chain worked: `SLACK_BOT_TOKEN` present in the running container's env,
and the restart's own reconciliation (OPEN-251) correctly resumed all 5 Fargate jobs that were
in flight at restart time (today's Sunday secondary-state batch) -- nothing orphaned.

**But the specific token (reused from `ddp-broker-py`'s own Slack app) does NOT have access to
`#automation-errors`.** Tested directly: `chat.postMessage` to both `#automation-errors` and
`automation-errors` returned `channel_not_found`. Couldn't enumerate the bot's visible channels
to confirm further -- this token also lacks `channels:read`/`groups:read` scope. This strongly
suggests `ddp-broker-py`'s Slack app lives in a different workspace or has never been added to
that channel -- reusing it was the wrong call, not a bug in the wiring itself.

**Still needed**: the real token `ddp-agents`' own `failure_watcher.py` actually uses for
`#automation-errors` (per your own note, it already listens there) -- if you or Ramon can
source that one, the mechanism is already fully wired and tested end-to-end; just swap the
secret's `slack_bot_token` value and restart `ddp-sync` again. Happy to re-verify once that
lands.
