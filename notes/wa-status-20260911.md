# wa status — both dry-runs clean, no commit yet

Both dry-runs done and clean, nothing committed:

- `refresh-extraction --dry-run`: `bills_with_stale_docs=3411 stale_docs=5818
  diffs_would_change=4538 docs_skipped=0 docs_refused=0` -- clean, no refused documents.
- `recompute-diff-order --dry-run`: `3411 bills checked | unchanged=7098 corrected=4538
  nulled=0` -- but this was taken *before* `refresh-extraction` is committed, so it's an
  early look only, not the final pre-commit number.

Per the plan's sequencing, still needed for `wa`: `refresh-extraction --commit` first, then
a **fresh** `recompute-diff-order --dry-run` (since the refresh changes underlying text),
spot-check sample, go-ahead, then `recompute-diff-order --commit`.

Queued behind `ut`'s now-corrected re-do (see
`notes/ut-tracking-error-acknowledged-20260911.md`) and `us refresh-extraction`'s still-
running dry-run. Nothing committed for `wa` yet.
