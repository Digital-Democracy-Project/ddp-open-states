# OPEN-285: PR #46 merged -- ready for deploy + a real clean run

`openstates-core` PR #46 (no-purge fix + AZ `people_admin_*` schema-crash fix) is merged.

That's all four original OPEN-285 items now addressed in some form -- the remaining one, purge
policy + AZ's crash, was code, not a decision, and it's shipped. Whenever you get a chance:
could you pull/deploy this on the EC2 host (same as PR #146 needed its own deploy earlier
today) and re-trigger `openstates_people_refresh` for real? If all 9 states complete clean this
time, that's the last piece needed to close OPEN-285 (and OPEN-193 right behind it).
