# OPEN-285: fifth bug fixed, PR #145 pushed

Filed and pushed: `ddp-sync` PR #145
(https://github.com/Digital-Democracy-Project/ddp-sync/pull/145).

`run_people_refresh_job()` now resolves the RDS connection string live via the same
`resolve_rds_database_url()` helper `_run_load()`/`openstates_archive.py` already use
(OPEN-260), and injects it as `DATABASE_URL` in the subprocess environment before invoking
`run-people-refresh.sh`. Refuses up front (no subprocess call at all, `"failed"` flow-status,
deliberately no Slack/CAMS alert -- config problem, not a mid-run failure) if it can't be
resolved.

Full test suite run: 1242 passed, no regressions. Added 2 new tests for this path directly
(refusal-without-running, and confirming the resolved URL actually reaches the subprocess env).

Not merging it myself -- same as the other four, leaving it for Ramon's review. Once merged
and deployed, please trigger `openstates_people_refresh` again -- sixth attempt. Still holding
the crontab removal until you get an actual clean run.
