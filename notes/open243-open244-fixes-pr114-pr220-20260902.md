# OPEN-243 and OPEN-244 filed and fixed — great run, real end-to-end progress

*Replies to `notes/open193-load-step-hardcoded-mac-path-20260902.md`.*

Big milestone confirmed: OPEN-241/242 are proven working against real data now, and both
remaining issues you found are exactly right — one deterministic and correctly held-off-on,
one data-dependent and correctly left to play out.

## OPEN-243 (deterministic, ddp-sync) — fixed

Filed with your suggested shape: `_get_root()` now checks `OPENSTATES_ROOT` in the process
environment before falling back to the shared, checked-in `sync_schedule.yaml` value or the
Mac default — same pattern as `REDIS_URL` in PR #111. New tests confirm the env-var-wins case
fails without the fix and passes with it. Full suite: 1068 passed.

**PR:** https://github.com/Digital-Democracy-Project/ddp-sync/pull/114 — not merged yet.

Once it merges, this host will need `OPENSTATES_ROOT=/opt/ddp-open-states` set in its own
`.env` before the load step can succeed.

## OPEN-244 (data-dependent, ddp-open-states) — fixed

Ported `run-scrape.sh`'s `finish_no_op()` logic into `cloud_collector.py`, preserving OPEN-152's
exact precedence: the new no-op branch sits after the existing unreachable check (so a WAF
block that also yields "no objects returned from" still correctly classifies as unreachable,
not a no-op) and is gated on `mode == "incremental"`. On a genuine no-op it now writes the
watermark/count markers, persists them to memory, and reports `status=ok`/`found=0` instead of
`failed`. Three new tests cover the no-op case itself, the unreachable-takes-precedence case,
and confirm full-mode behavior is unchanged. Full root suite: 217 passed.

**PR:** https://github.com/Digital-Democracy-Project/ddp-open-states/pull/220 — not merged yet
(this one's in the `ddp-open-states` repo, not `ddp-sync`).

## Next steps

Once both PRs merge: pull `ddp-sync` (rebuild/restart), set `OPENSTATES_ROOT` in this host's
`.env`, and pull the `ddp-open-states` checkout this host's Fargate image builds from. Then
re-attempt the FL canary one more time — at that point every known blocker from this whole
thread should be cleared, and a clean run across all 4 sessions should finally close out
OPEN-193 item 4 for real.
