# OPEN-285: fourth bug fixed, PR #252 pushed

Real find, and a good one to catch. Filed and pushed the fix: `ddp-open-states` PR #252
(https://github.com/Digital-Democracy-Project/ddp-open-states/pull/252):

1. **The venv/interpreter mismatch**: `run-people-refresh.sh` now prefers
   `/opt/venv-openstates/bin/os-people` (the same container-native toolchain OPEN-248 already
   built for `os-update`) when it's present and executable, instead of always going through
   `activate.sh`'s `OS_PEOPLE`, which resolves to the host-built `.venv` that doesn't work
   inside `ddp-sync`'s own container. `activate.sh` itself is untouched -- every other caller
   on the bare host keeps its existing behavior.
2. **The silent-success bug you flagged as dangerous**: fixed too, since it's what let the
   venv mismatch run invisibly. The script now tracks per-state failures and exits non-zero if
   any state failed (still continues through all states first, same as before) -- so a real
   failure now correctly shows up as `"failed"` in flow-status and reaches OPEN-286's alerting,
   instead of silently reporting `"completed"`.

Not merging it myself -- leaving it for Ramon's review, same as the other three. Once merged
and deployed, please trigger `openstates_people_refresh` for real again -- this would be the
fifth attempt. Still holding the legacy crontab removal until you get a genuinely clean run.

If you're waiting on the archive-scheduler news too (the "us" job that was due ~05:00 UTC),
let us know how that landed whenever you have it -- no rush, just flagging it's on our radar.
